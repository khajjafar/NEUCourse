# NEUCourse Testing Documentation

A comprehensive guide to the testing strategy, coverage results, and test inventory for graders and contributors who want to understand or run the test suite.

---

## 1. Testing Overview

NEUCourse uses three testing layers:

| Layer | Tool | Scope |
|---|---|---|
| Unit / Integration | Vitest + React Testing Library | API route handlers, custom hooks, React components, page components, library utilities |
| End-to-End | Playwright | Full browser flows (course search, auth, calendar, navigation) |

**Why these tools?**
- **Vitest** integrates natively with the Vite-based Next.js build toolchain, runs fast in watch mode, and provides first-class TypeScript support without extra configuration.
- **React Testing Library** encourages testing user-visible behavior rather than implementation details - we test what users see and interact with, not internal state.
- **Playwright** runs against the real Next.js dev server, giving confidence that pages render, routes resolve, and browser interactions work end-to-end.

Coverage is enforced at **80% for all four metrics** (statements, branches, functions, lines) via `vitest.config.ts`. The CI build fails if any metric drops below this threshold.

---

## 2. Quick Start - Run Tests Yourself

```bash
# Install dependencies
npm install

# Run all unit + integration tests
npm run test

# Run tests with coverage report (outputs to coverage/ directory)
npm run coverage

# Open the pre-generated HTML coverage report in your browser (macOS)
npm run coverage:open

# Run E2E tests (requires .env.test with TEST_USER_EMAIL + TEST_USER_PASSWORD)
npm run test:e2e
```

> **Note on E2E tests:** Playwright starts the Next.js dev server automatically (`npm run dev` on port 3000). Tests that require authenticated state use `test.skip()` unless `.env.test` is configured - see [Section 5](#5-end-to-end-tests-playwright) for details.

---

## 3. Coverage Summary

Coverage was last measured on 2026-03-09 after Sprint 2 was complete.

| Metric | Current | Threshold |
|---|---|---|
| Statements | **92.66%** (973/1050) | 80% |
| Branches | **80.22%** (560/698) | 80% |
| Functions | **86.77%** (164/189) | 80% |
| Lines | **94.56%** (923/976) | 80% |

All four metrics exceed the 80% threshold enforced in `vitest.config.ts` and the GitHub Actions CI pipeline.

The full interactive HTML report is generated by running `npm run coverage` and is located at `coverage/index.html`. It includes file-by-file breakdowns, uncovered line highlighting, and branch coverage visualization.

---

## 4. Unit & Integration Tests (Vitest + React Testing Library)

28 test files covering API routes, hooks, components, and pages.

### API Route Handlers (`app/api/v1/**/*.test.ts`)

#### `app/api/v1/plans/route.test.ts`
Tests the GET (list plans) and POST (create plan) endpoints.
- Returns 401 when the Authorization header is missing or invalid
- Returns an empty array when the user has no plans
- Returns plans with a `semesterCount` field derived from the semesters subcollection
- POST returns 400 when `name` is missing, whitespace-only, or not a string
- POST returns 201 with the created plan on success
- Returns 500 on unexpected Firestore errors

#### `app/api/v1/plans/[planId]/route.test.ts`
Tests GET (fetch single plan with semesters), PUT (rename), and DELETE.
- GET returns 401 for unauthenticated requests
- GET returns the plan with its semesters array populated
- PUT validates that the new name is a non-empty string
- PUT returns the updated plan on success
- DELETE removes the plan and returns 200
- Returns 500 on Firestore failures

#### `app/api/v1/plans/[planId]/semesters/route.test.ts`
Tests POST (create semester).
- Returns 401 for unauthenticated requests
- Returns 400 when `name` is missing
- Returns 201 with the created semester on success

#### `app/api/v1/plans/[planId]/semesters/[semId]/courses/route.test.ts`
Tests POST (add course to semester).
- Returns 401 for unauthenticated requests
- Returns 400 when `courseId` is missing
- Supports an optional `crn` field
- Returns 200 on success

#### `app/api/v1/plans/[planId]/semesters/[semId]/courses/[courseId]/route.test.ts`
Tests DELETE (remove course from semester).
- Returns 401 for unauthenticated requests
- Removes the course by matching courseId in both string and CourseAssignment format
- Returns 200 on success

#### `app/api/v1/events/route.test.ts`
Tests GET (list events) and POST (create event).
- Returns 401 for unauthenticated requests
- GET returns the user's events sorted by startTime
- POST returns 400 when required fields (`title`, `startTime`, `endTime`) are missing
- POST returns 201 with the created event on success
- Returns 500 on Firestore errors

#### `app/api/v1/events/[eventId]/route.test.ts`
Tests PUT (update event) and DELETE (remove event).
- Returns 401 for unauthenticated requests
- PUT validates all required fields are present
- PUT returns the updated event on success
- DELETE removes the event and returns 200

#### `app/api/v1/courses/route.test.ts`
Tests GET (search courses - public, no auth required).
- Accepts `q`, `subject`, `minLevel`, and `maxLevel` query parameters
- Returns up to 50 matching courses from Firestore
- Returns 500 on Firestore errors

#### `app/api/v1/courses/[courseId]/route.test.ts`
Tests GET (single course - public, no auth required).
- Returns the full course document including sections
- Returns 404 when the courseId does not exist
- Returns 500 on Firestore errors

#### `app/api/v1/profile/profile.test.ts`
Tests GET (fetch profile) and PATCH (update requirements).
- GET returns 401 for unauthenticated requests
- GET returns the user's profile with graduation requirements
- PATCH updates the requirements array and returns 200
- Returns 500 on Firestore errors

---

### Hooks (`hooks/*.test.ts`)

#### `hooks/useAuth.test.tsx`
Tests the Firebase authentication context hook.
- Returns loading=true initially, then resolves after auth check
- Updates user state when Firebase auth state changes
- `logout()` calls `signOut` and is accessible from the context

#### `hooks/usePlans.test.ts`
Tests degree plan CRUD operations with optimistic updates.
- Returns empty plans array when user is unauthenticated
- Fetches plans from GET /api/v1/plans on mount
- Handles API error responses gracefully
- `createPlan()` posts to the API and refetches
- `deletePlan()` removes the plan optimistically from local state
- `renamePlan()` updates local state on success
- Handles null data responses and unknown error types

#### `hooks/useEvents.test.ts`
Tests calendar event CRUD operations.
- `addEvent()` sends a POST with the Authorization header
- `updateEvent()` sends a PUT and refetches events
- `deleteEvent()` removes the event from local state
- All methods throw when the user is not authenticated
- Handles network failures and null data responses

#### `hooks/useCourseSearch.test.ts`
Tests debounced course search.
- Sends fetch with `q`, `subject`, and `minLevel` query params
- Handles API error responses

#### `hooks/usePlanDetails.test.ts`
Tests plan detail management with semester and course operations.
- Returns null plan when planId or auth is missing
- Fetches plan details from GET /api/v1/plans/[planId]
- `addSemester()` posts and refetches
- `addCourseToSemester()` performs optimistic UI update with CRN support
- `removeCourseFromSemester()` removes optimistically and reverts on failure
- `deleteSemester()` performs optimistic removal
- `reorderSemesters()` handles edge cases (same-index no-op)
- `moveCourseBetweenSemesters()` handles within-semester and cross-semester moves
- `updateCourseAssignment()` updates requirement mapping

#### `hooks/useSingleCourse.test.ts`
Tests single course fetching with caching.
- Does not fetch when `courseId` is null
- Fetches and updates course data from the API
- Returns from cache on second mount (no redundant fetch)
- Deduplicates simultaneous requests via `inFlightPromises`
- Handles 404, 5xx, and unknown errors

#### `hooks/useUserProfile.test.ts`
Tests graduation requirements profile management.
- Returns null profile when user is unauthenticated
- Fetches profile from GET /api/v1/profile
- `updateRequirements()` performs optimistic update and reverts on failure
- Handles null data responses and unknown errors

---

### Components (`components/*.test.tsx`)

#### `components/AddToCalendarButton.test.tsx`
Tests the button that creates calendar events from course sections.
- Renders button with `aria-label="Add course schedule to calendar"`
- Shows inline error when the section CRN is not found
- Shows "No schedule available" error when meeting time is TBA
- Calls `addEvent()` for each parsed meeting day on success
- Displays success message after all events are created

#### `components/AddScheduleToCalendarModal.test.tsx`
Tests the bulk schedule-to-calendar modal.
- Renders nothing when `isOpen` is false
- Shows instructional message when no courses have a CRN selected
- Only lists courses that have a pre-selected section (CRN)
- Each listed course has an AddToCalendarButton

#### `components/CourseDetailClient.test.tsx`
Tests the client-side course detail view.
- Shows animated loading skeleton while fetching
- Shows error message when the course is not found
- Renders course name, credit hours, description, and prerequisites
- Calls `router.back()` when the back button is clicked

#### `components/CourseMiniCard.test.tsx`
Tests the compact course card used in the plan builder.
- Shows "Prereq needed" badge when a prerequisite has not been taken earlier
- Badge includes `aria-label` listing the missing prereqs for screen readers
- Does not show badge when all prerequisites are satisfied
- Shows "Coreq needed" badge for missing co-requisites
- Shows no warnings when the course has no prereqs or coreqs
- Renders the requirement assignment dropdown when requirements are provided

#### `components/GraduationRequirementsModal.test.tsx`
Tests the graduation requirements CRUD modal.
- Renders nothing when `isOpen` is false
- Displays existing requirements when open
- Adds a new requirement via the form
- Removes a requirement via the trash button
- Saves changes by calling `updateRequirements` and closes the modal

---

### Pages (`app/**/*.test.tsx`)

#### `app/login/login.test.tsx`
Tests the email/password login page.
- Renders email, password inputs and the sign-in button
- Calls `signInWithEmailAndPassword` with the provided credentials on submit
- Shows a generic error message on authentication failure (never reveals which field was wrong)

#### `app/register/register.test.tsx`
Tests the new account registration page.
- Validates that the password is at least 8 characters before submitting
- Shows "email already in use" error for duplicate accounts
- Calls `createUserWithEmailAndPassword` on valid submission

#### `app/dashboard/dashboard.test.tsx`
Tests the authenticated student dashboard.
- Shows skeleton loaders while data is loading
- Displays empty state messages when there are no plans or events
- Displays plan cards with semester count and last-updated timestamp
- Filters and displays only upcoming events within the next 7 days

#### `app/courses/courses.test.tsx`
Tests the course search and browse page.
- Shows a loading spinner while courses are being fetched
- Displays an empty state when no courses match the search
- Renders a list of courses with credit hours and prerequisite info
- Updates search and subject filter state on input changes

---

## 5. End-to-End Tests (Playwright)

4 spec files in the `tests/` directory, run against the Next.js dev server on `http://localhost:3000`.

**Auth-required tests:** Tests that require a logged-in user use `test.skip()` unless `.env.test` is present with `TEST_USER_EMAIL` and `TEST_USER_PASSWORD`. This is intentional - the skip is not a broken test, it's a guard against running against a real Firebase project without credentials.

**Public-route tests:** Tests on public pages (login, register, courses) run headlessly without any credentials.

#### `tests/auth.spec.ts`
Tests authentication UI elements on public pages.
- Login page renders email input, password input, and sign-in button
- Login page has a link to the guest/browse view
- Register page renders all form fields and sign-up button

#### `tests/courses.spec.ts`
Tests the public course search and detail flow.
- Courses page loads and displays the search input and subject filter
- Clicking a course card opens the course detail view (intercepting route / modal)
- Course detail shows course name, credit hours, and prerequisites
- Pressing Escape closes the detail view and restores the URL

#### `tests/calendar.spec.ts`
Tests authenticated calendar event management.
- Weekly calendar grid renders with time slots
- User can create a new event via the quick-add button
- Created event appears on the correct day/time slot
- User can edit an existing event's title and time
- User can delete an event (with confirmation dialog)

#### `tests/navigation.spec.ts`
Tests page navigation and routing.
- Nav links (Dashboard, Plans, Courses, Calendar) navigate to the correct pages
- Unauthenticated access to protected routes redirects to /login
- Back-navigation works correctly after visiting a detail page

---

## 6. What We Tested and Why

The test suite prioritizes API route handlers and custom hooks because that is where all the business logic lives. Route handlers own authentication enforcement (`verifyAuth()`), input validation, and Firestore operations - a bug here could expose or corrupt user data. Hooks own state management and data fetching - testing them ensures the UI has accurate data and that optimistic updates revert correctly on failure.

Mocking strategy: we use `vi.mock()` to mock the `firebase` and `firebase-admin` modules, replacing them with stubs that return predictable data. We mock `global.fetch` to simulate API responses without hitting the network. This means unit tests run in milliseconds and never touch real Firestore, making them safe to run anywhere. The `AuthProvider` wrapper in hook tests provides the `useAuth()` context without requiring a real Firebase auth session.

The 80% coverage threshold is enforced by both `vitest.config.ts` and the GitHub Actions CI workflow. If a pull request drops any metric below 80%, the coverage step fails and the PR cannot be merged until coverage is restored. This keeps the quality bar clear and measurable rather than aspirational.

---

## 7. CI Integration

Tests run automatically on every push and pull request via **GitHub Actions** (`.github/workflows/ci.yml`).

The pipeline runs these steps in order:

1. **Lint** - ESLint with TypeScript rules
2. **Coverage** - `npm run coverage` with Vitest threshold enforcement (fails if any metric < 80%)
3. **Security audit** - `npm audit` (fails on high-severity vulnerabilities)
4. **Build** - `npm run build` to catch TypeScript and build errors

Coverage artifacts (the HTML report) are uploaded to GitHub Actions artifacts on every run, so you can download and browse them without running locally.

For pull requests, Vercel posts a preview deployment URL as a PR comment (requires `VERCEL_TOKEN`, `VERCEL_ORG_ID`, and `VERCEL_PROJECT_ID` secrets). Pushes to `main` trigger a production deployment automatically.
